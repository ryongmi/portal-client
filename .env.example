# 포탈 클라이언트 환경 설정 파일
# 이 파일을 .env.local로 복사하고 환경에 맞게 값을 수정하세요

# ========================================
# 서버 URL 설정
# ========================================

# 인증 서버 URL - 사용자 로그인/회원가입 처리
NEXT_PUBLIC_AUTH_SERVER_URL=http://auth.krgeobuk.local:8000/api

# 인가 서버 URL - 역할 및 권한 관리
NEXT_PUBLIC_AUTHZ_SERVER_URL=http://authz.krgeobuk.local:8100/api

# 포탈 서버 URL - 서비스 등록 및 관리
NEXT_PUBLIC_PORTAL_SERVER_URL=http://portal.krgeobuk.local:8200/api

# 포탈 클라이언트 URL (현재 앱) - 이 포탈 클라이언트의 URL
NEXT_PUBLIC_PORTAL_CLIENT_URL=http://portal.krgeobuk.local:3200


# ========================================
# 환경 설정
# ========================================

# 애플리케이션 환경 (local, development, production)
NEXT_PUBLIC_ENVIRONMENT=local
NODE_ENV=local

# ========================================
# Next.js 설정
# ========================================

# Next.js 텔레메트리 비활성화 (프라이버시 보호)
NEXT_TELEMETRY_DISABLED=1

# ========================================
# 선택적 설정 (필요시 주석 해제)
# ========================================

# 디버그 모드 활성화 여부 (true/false)
# NEXT_PUBLIC_DEBUG=false

# 로그 레벨 설정 (error, warn, info, debug)
# NEXT_PUBLIC_LOG_LEVEL=info

# ========================================
# 프로덕션 환경 배포시 참고사항
# ========================================
# 실제 운영 환경에서는 다음과 같이 실제 도메인으로 변경하세요:

# 서버 URL 설정
# NEXT_PUBLIC_AUTH_SERVER_URL=https://auth.yourdomain.com/api
# NEXT_PUBLIC_AUTHZ_SERVER_URL=https://authz.yourdomain.com/api
# NEXT_PUBLIC_PORTAL_SERVER_URL=https://portal-api.yourdomain.com/api
# NEXT_PUBLIC_PORTAL_CLIENT_URL=https://portal.yourdomain.com
# NEXT_PUBLIC_ENVIRONMENT=production
# NODE_ENV=production

# HTTP 클라이언트 프로덕션 설정 (@krgeobuk/http-client)
# ※ 아래 설정들은 기본값이 있어 로컬에서는 생략 가능하지만, 운영에서는 명시적 설정 권장

# 허용된 도메인 목록 (보안 정책용) - 쉼표로 구분
# 기본값: localhost,127.0.0.1,krgeobuk.com → 운영 도메인으로 변경 필요
# ALLOWED_ORIGINS=yourdomain.com,auth.yourdomain.com,authz.yourdomain.com,portal-api.yourdomain.com

# API 요청 타임아웃 시간 (밀리초) - 기본값: 10000ms → 운영에서는 15초 권장
# NEXT_PUBLIC_API_TIMEOUT=15000

# 레이트 리미터 설정 - 운영에서는 더 엄격하게 제한 가능
# 최대 요청 횟수 (기본값: 100) → 운영에서는 50 권장
# NEXT_PUBLIC_RATE_LIMIT_MAX_ATTEMPTS=50
# 시간 윈도우 (기본값: 60000ms)
# NEXT_PUBLIC_RATE_LIMIT_WINDOW_MS=60000

# 보안 설정 (모든 기본값: true) - 운영에서는 반드시 활성화
# CSRF 보호 활성화
# NEXT_PUBLIC_ENABLE_CSRF=true
# 입력값 검증 활성화
# NEXT_PUBLIC_ENABLE_INPUT_VALIDATION=true
# 보안 로깅 활성화
# NEXT_PUBLIC_ENABLE_SECURITY_LOGGING=true